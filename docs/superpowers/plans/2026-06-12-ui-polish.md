# 界面打磨优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在经典桌面界面基础上完成高分屏适配、程序图标与窗口居中、提示条/横幅左色条样式、日志分级着色与行数上限、日志右键菜单、等待人工处理时任务栏闪烁。

**Architecture:** 全部改动集中在 `app.py`：新增模块级纯函数（可单测）+ `App` 类内的界面接线。Windows API（DPI、闪烁）全部经 `ctypes` 调用并以 try/except 降级，非 Windows 平台静默跳过。不改 `Assistant`、`runtime_installer`、`app_logging`。

**Tech Stack:** Python 标准库（tkinter/ttk、ctypes、base64 内嵌 PNG）、pytest。

**提交规范：** 遵循项目 git-commit-guidelines（`<type>(<scope>): <subject>`）。本计划全部改动属 `service` 范畴（app.py 界面层），实现与对应测试同 commit（与仓库既有惯例一致，参见 efee8e8）。

**测试命令：** 仓库根目录运行 `python -m pytest tests/test_app_runtime.py -v`。

---

### Task 1: 高分屏 DPI 感知与缩放

**Files:**
- Modify: `app.py`（模块级函数 + `__init__` + `__main__`）
- Test: `tests/test_app_runtime.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_app_runtime.py` 顶部 import 行改为（后续任务会继续往这一行加名字，每次以当前文件实际内容为准）：

```python
from app import (
    enable_dpi_awareness,
    log_toggle_text,
    runtime_button_state,
    scaled_size,
    status_presentation,
)
```

文件末尾追加：

```python
def test_scaled_size_scales_dimensions():
    assert scaled_size(780, 500, 1.5) == (1170, 750)


def test_scaled_size_never_shrinks_below_base():
    assert scaled_size(780, 500, 0.8) == (780, 500)


def test_scaled_size_caps_extreme_factor():
    assert scaled_size(100, 100, 10.0) == (300, 300)


def test_enable_dpi_awareness_does_not_raise():
    enable_dpi_awareness()
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: FAIL，`ImportError: cannot import name 'scaled_size'`

- [ ] **Step 3: 实现**

在 `app.py` 中 `import os` 之前加 `import ctypes`（保持 import 字母序）。在 `STATUS_COLORS` 定义之后、`runtime_button_state` 之前加：

```python
def enable_dpi_awareness():
    """启用 Windows DPI 感知，高分屏下界面不再发虚；失败时静默降级。"""
    try:
        ctypes.windll.shcore.SetProcessDpiAwareness(1)
    except Exception:
        try:
            ctypes.windll.user32.SetProcessDPIAware()
        except Exception:
            pass


def scaled_size(width: int, height: int, factor: float):
    factor = max(1.0, min(factor, 3.0))
    return round(width * factor), round(height * factor)
```

`App.__init__` 中把：

```python
        self.geometry("780x500")
        self.minsize(720, 430)
```

替换为：

```python
        dpi = self.winfo_fpixels("1i")
        self.tk.call("tk", "scaling", dpi / 72.0)
        self._ui_scale = dpi / 96.0
        width, height = scaled_size(780, 500, self._ui_scale)
        self.geometry(f"{width}x{height}")
        self.minsize(*scaled_size(720, 430, self._ui_scale))
```

`__main__` 入口改为：

```python
if __name__ == "__main__":
    enable_dpi_awareness()
    App().mainloop()
```

注意 `enable_dpi_awareness()` 必须在创建 `Tk` 之前调用才生效。

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add app.py tests/test_app_runtime.py
git commit -m "feat(service): 适配高分屏 DPI 缩放"
```

---

### Task 2: 程序图标与窗口居中

**Files:**
- Modify: `app.py`
- Test: `tests/test_app_runtime.py`

- [ ] **Step 1: 写失败测试**

import 行加入 `centered_geometry`。文件末尾追加：

```python
def test_centered_geometry_centers_horizontally():
    assert centered_geometry(800, 500, 1920, 1080) == "800x500+560+232"


def test_centered_geometry_clamps_to_screen_origin():
    assert centered_geometry(2000, 1500, 1920, 1080) == "2000x1500+0+0"
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: FAIL，`ImportError: cannot import name 'centered_geometry'`

- [ ] **Step 3: 实现**

`app.py` 模块级、`scaled_size` 之后加（垂直位置取 2/5 处，略高于正中，符合桌面程序惯例）：

```python
def centered_geometry(width: int, height: int, screen_width: int, screen_height: int):
    x = max((screen_width - width) // 2, 0)
    y = max((screen_height - height) * 2 // 5, 0)
    return f"{width}x{height}+{x}+{y}"
```

`STATUS_COLORS` 之前加图标数据（base64 PNG，蓝底白色播放三角，16/32/64 三档）：

```python
APP_ICON_PNGS = (
    "iVBORw0KGgoAAAANSUhEUgAAABAAAAAQCAYAAAAf8/9hAAAAN0lEQVR42mNggALL6v3/ScEMyIBUzSiGkKsZbsgwNwAGKDYAnyFEG4DLIPq4YLinA4ozE6XZGQDQdGvd6ZELaQAAAABJRU5ErkJggg==",
    "iVBORw0KGgoAAAANSUhEUgAAACAAAAAgCAYAAABzenr0AAAAa0lEQVR42mNgwAIsq/f/pwVmwAdoZSlRjqG35RiOGNkOGCjL4Y4YdcCoA8jRBAID7gAYGHAHUOoIqjiAEodQ1QHkOILqDiDVITRzALEOGX4hMDJzwcgsCUer41EHDIuW8WjHZPB0Tgeyew4AglrT/kuv/40AAAAASUVORK5CYII=",
    "",
)
```

`App.__init__` 中把 Task 1 写的：

```python
        self.geometry(f"{width}x{height}")
```

替换为：

```python
        self.geometry(
            centered_geometry(
                width, height, self.winfo_screenwidth(), self.winfo_screenheight()
            )
        )
        self._icon_images = [tk.PhotoImage(data=png) for png in APP_ICON_PNGS]
        self.iconphoto(True, *self._icon_images)
```

`self._icon_images` 必须存为实例属性，否则 PhotoImage 被垃圾回收后图标失效。

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add app.py tests/test_app_runtime.py
git commit -m "feat(service): 添加程序图标并居中启动窗口"
```

---

### Task 3: 提示条与警示横幅左色条样式

**Files:**
- Modify: `app.py`（`_build`、`_update_banner`）

纯布局改动，无新纯函数，无新测试；以现有测试不回归 + 手动验收为准。

- [ ] **Step 1: 重写横幅**

`_build` 中把：

```python
        self.banner = tk.Label(
            self.main,
            text="",
            fg="#a13a32",
            bg="#fbeceb",
            highlightbackground="#e3b9b6",
            highlightthickness=1,
            font=("Microsoft YaHei UI", 10, "bold"),
            padx=10,
            pady=8,
            anchor="w",
        )
```

替换为（外层 Frame 的背景色透出，左侧 4px 红色色条 + 四周 1px 边线）：

```python
        self.banner_wrap = tk.Frame(self.main, bg="#c0453c")
        self.banner = tk.Label(
            self.banner_wrap,
            text="",
            fg="#8f2f27",
            bg="#fbeceb",
            font=("Microsoft YaHei UI", 10, "bold"),
            padx=10,
            pady=8,
            anchor="w",
        )
        self.banner.pack(fill="x", padx=(4, 1), pady=1)
```

- [ ] **Step 2: 重写提示条**

`_build` 中把：

```python
        self.hint_label = tk.Label(
            self.main,
            text="提示：首次启动后，请在浏览器中手动登录。遇到答题时程序会暂停并提醒。",
            fg="#715b20",
            bg="#fff8e6",
            highlightbackground="#e6d29b",
            highlightthickness=1,
            font=("Microsoft YaHei UI", 9),
            padx=10,
            pady=7,
            anchor="w",
        )
        self.hint_label.pack(fill="x", pady=(10, 0))
```

替换为：

```python
        hint_wrap = tk.Frame(self.main, bg="#d8b54a")
        self.hint_label = tk.Label(
            hint_wrap,
            text="提示：首次启动后，请在浏览器中手动登录。遇到答题时程序会暂停并提醒。",
            fg="#715b20",
            bg="#fff8e6",
            font=("Microsoft YaHei UI", 9),
            padx=10,
            pady=7,
            anchor="w",
        )
        self.hint_label.pack(fill="x", padx=(4, 1), pady=1)
        hint_wrap.pack(fill="x", pady=(10, 0))
```

- [ ] **Step 3: 统一面板边距**

`_build_toolbar` 中 `padding=(8, 6)` 改为 `padding=(10, 6)`；`_build_log_panel` 中 `padding=(8, 6)` 改为 `padding=(12, 8)`，与运行概览面板的 `padding=(12, 10)` 视觉对齐。

- [ ] **Step 4: 改横幅显隐逻辑**

`_update_banner` 整体替换为（pack/forget 改为操作外层 wrap）：

```python
    def _update_banner(self, status):
        if status in ("等待答题", "等待人工处理"):
            self.banner.configure(text=f"⚠  {status}：请到浏览器手动处理")
            self.banner_wrap.pack(fill="x", pady=(0, 10), before=self.overview)
        else:
            self.banner_wrap.pack_forget()
```

- [ ] **Step 4: 运行测试确认不回归**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: 全部 PASS

- [ ] **Step 5: 手动冒烟**

Run: `python app.py`（启动看一眼即关）
Expected: 黄色提示条左侧有 4px 金色色条；窗口居中、有蓝色图标。

- [ ] **Step 6: 提交**

```bash
git add app.py
git commit -m "feat(service): 重绘提示条与警示横幅为左色条样式"
```

---

### Task 4: 日志分级着色与行数上限

**Files:**
- Modify: `app.py`（模块级函数、`_build_log_panel`、`_drain_events`）
- Test: `tests/test_app_runtime.py`

- [ ] **Step 1: 写失败测试**

import 行加入 `log_line_kind, log_overflow`。文件末尾追加：

```python
def test_log_line_kind_flags_errors():
    assert log_line_kind("运行失败：超时") == "error"
    assert log_line_kind("浏览器组件安装失败：网络错误") == "error"


def test_log_line_kind_flags_warnings():
    assert log_line_kind("等待答题，请到浏览器手动处理") == "warning"
    assert log_line_kind("已暂停播放") == "warning"


def test_log_line_kind_defaults_to_info():
    assert log_line_kind("开始播放第 3 课") == "info"


def test_log_overflow_below_limit_is_zero():
    assert log_overflow(1000, 1000) == 0


def test_log_overflow_counts_excess_lines():
    assert log_overflow(1003, 1000) == 3
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: FAIL，`ImportError: cannot import name 'log_line_kind'`

- [ ] **Step 3: 实现纯函数**

`app.py` 模块级，`STATUS_COLORS` 之后加：

```python
LOG_MAX_LINES = 1000
LOG_LINE_COLORS = {"error": "#a13a32", "warning": "#8a6d1f"}


def log_line_kind(message: str) -> str:
    if any(word in message for word in ("失败", "错误", "异常")):
        return "error"
    if any(word in message for word in ("等待", "暂停", "请")):
        return "warning"
    return "info"


def log_overflow(total_lines: int, max_lines: int = LOG_MAX_LINES) -> int:
    return max(total_lines - max_lines, 0)
```

- [ ] **Step 4: 接入界面**

`_build_log_panel` 末尾（`self.log_box.pack(...)` 之后）加：

```python
        for tag, color in LOG_LINE_COLORS.items():
            self.log_box.tag_configure(tag, foreground=color)
```

`App` 类中新增方法：

```python
    def _append_log(self, message):
        self.log_box.configure(state="normal")
        self.log_box.insert(
            "end",
            time.strftime("[%H:%M:%S] ") + message + "\n",
            log_line_kind(message),
        )
        total_lines = int(self.log_box.index("end-1c").split(".")[0]) - 1
        overflow = log_overflow(total_lines)
        if overflow:
            self.log_box.delete("1.0", f"{overflow + 1}.0")
        self.log_box.see("end")
        self.log_box.configure(state="disabled")
```

`_drain_events` 中把：

```python
            if kind == "log":
                self.log_box.configure(state="normal")
                self.log_box.insert("end", time.strftime("[%H:%M:%S] ") + value + "\n")
                self.log_box.see("end")
                self.log_box.configure(state="disabled")
                self.last_activity_var.set(f"最近活动：{value}")
```

替换为：

```python
            if kind == "log":
                self._append_log(value)
                self.last_activity_var.set(f"最近活动：{value}")
```

说明：`info` 不入 `LOG_LINE_COLORS`，普通行沿用 Text 默认前景色 `#243244`；`log_line_kind` 返回的 "info" 作为未配置的 tag 名传给 insert 是无害的。

- [ ] **Step 5: 运行确认通过**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
git add app.py tests/test_app_runtime.py
git commit -m "feat(service): 日志分级着色并限制最大行数"
```

---

### Task 5: 日志右键菜单

**Files:**
- Modify: `app.py`（`_build_log_panel` + 新方法）

界面交互接线，无纯函数；以现有测试不回归 + 手动验收为准。

- [ ] **Step 1: 实现**

`_build_log_panel` 末尾（tag 配置之后）加：

```python
        self.log_menu = tk.Menu(self.log_box, tearoff=False)
        self.log_menu.add_command(label="复制", command=self.copy_log)
        self.log_menu.add_command(label="全选", command=self.select_all_log)
        self.log_menu.add_separator()
        self.log_menu.add_command(label="清空日志", command=self.clear_log)
        self.log_box.bind("<Button-3>", self._show_log_menu)
```

`App` 类中新增方法（放在 `toggle_log` 之前）：

```python
    def _show_log_menu(self, event):
        self.log_menu.tk_popup(event.x_root, event.y_root)

    def copy_log(self):
        try:
            text = self.log_box.get("sel.first", "sel.last")
        except tk.TclError:
            text = self.log_box.get("1.0", "end-1c")
        if text:
            self.clipboard_clear()
            self.clipboard_append(text)

    def select_all_log(self):
        self.log_box.focus_set()
        self.log_box.tag_add("sel", "1.0", "end-1c")

    def clear_log(self):
        self.log_box.configure(state="normal")
        self.log_box.delete("1.0", "end")
        self.log_box.configure(state="disabled")
        self.last_activity_var.set("最近活动：暂无")
```

说明：`copy_log` 无选中时复制全部；`clear_log` 只清界面，`app.log` 文件不受影响（文件写入在 `app_logging`，与 Text 控件无关）。

- [ ] **Step 2: 运行测试确认不回归**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: 全部 PASS

- [ ] **Step 3: 手动冒烟**

Run: `python app.py`，展开详细日志，右键日志区。
Expected: 弹出 复制/全选/清空日志 菜单，三项均可用。

- [ ] **Step 4: 提交**

```bash
git add app.py
git commit -m "feat(service): 添加日志右键复制与清空菜单"
```

---

### Task 6: 等待人工处理时任务栏闪烁

**Files:**
- Modify: `app.py`（模块级 + `_update_banner`）
- Test: `tests/test_app_runtime.py`

- [ ] **Step 1: 写失败测试**

import 行加入 `should_flash`。文件末尾追加：

```python
def test_should_flash_for_manual_statuses():
    assert should_flash("等待答题") is True
    assert should_flash("等待人工处理") is True


def test_should_not_flash_for_normal_statuses():
    assert should_flash("就绪") is False
    assert should_flash("运行正常") is False
    assert should_flash("运行失败") is False
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: FAIL，`ImportError: cannot import name 'should_flash'`

- [ ] **Step 3: 实现**

`app.py` 模块级加（放在 `log_overflow` 之后）：

```python
MANUAL_STATUSES = ("等待答题", "等待人工处理")


class FLASHWINFO(ctypes.Structure):
    _fields_ = [
        ("cbSize", ctypes.c_uint),
        ("hwnd", ctypes.c_void_p),
        ("dwFlags", ctypes.c_uint),
        ("uCount", ctypes.c_uint),
        ("dwTimeout", ctypes.c_uint),
    ]


def should_flash(status: str) -> bool:
    return status in MANUAL_STATUSES
```

`App` 类中新增方法：

```python
    def _flash_taskbar(self):
        """任务栏闪烁直至窗口获得焦点；非 Windows 平台静默跳过。"""
        try:
            hwnd = ctypes.windll.user32.GetParent(self.winfo_id())
            flashw_all, flashw_timernofg = 0x3, 0xC
            info = FLASHWINFO(
                ctypes.sizeof(FLASHWINFO),
                hwnd,
                flashw_all | flashw_timernofg,
                0,
                0,
            )
            ctypes.windll.user32.FlashWindowEx(ctypes.byref(info))
        except Exception:
            pass
```

`_update_banner` 改为复用 `should_flash` 并触发闪烁：

```python
    def _update_banner(self, status):
        if should_flash(status):
            self.banner.configure(text=f"⚠  {status}：请到浏览器手动处理")
            self.banner_wrap.pack(fill="x", pady=(0, 10), before=self.overview)
            self._flash_taskbar()
        else:
            self.banner_wrap.pack_forget()
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_app_runtime.py -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add app.py tests/test_app_runtime.py
git commit -m "feat(service): 等待人工处理时闪烁任务栏提醒"
```

---

### Task 7: 全量回归与手动验收

**Files:** 无新改动。

- [ ] **Step 1: 全量测试**

Run: `python -m pytest tests/ -v`
Expected: 全部 PASS（含 test_assistant、test_page_logic 等既有用例）

- [ ] **Step 2: 手动验收清单**

Run: `python app.py`，逐项核对：

1. 窗口居中启动，标题栏与任务栏显示蓝色播放图标（非 Tk 羽毛）。
2. 高分屏（125%/150% 缩放）下文字锐利不发虚，窗口尺寸随缩放等比放大。
3. 黄色提示条左侧有金色色条；模拟等待答题状态时红色横幅出现且左侧有红色色条。
4. 展开详细日志：含"失败"的行红色、含"等待/请"的行琥珀色、普通行深灰。
5. 日志区右键菜单可复制、全选、清空；清空后 `logs/app.log` 文件内容仍在。
6. 把窗口切到后台，状态进入"等待答题"时任务栏图标闪烁，点击窗口后停止。

- [ ] **Step 3: 同步使用说明（如有）**

若 `docs/` 中的桌面界面使用说明提及日志或提醒行为，补充右键菜单与任务栏闪烁说明，单独提交：

```bash
git add docs/
git commit -m "docs(docs): 补充界面打磨后的使用说明"
```
